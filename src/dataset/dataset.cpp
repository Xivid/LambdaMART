#include <lambdamart/dataset.h>

namespace LambdaMART {

Dataset* load_dataset(const char* data_path, const char* query_path, Dataset* use_binning_from) {
    if (use_binning_from) {
        // TODO
    } else {
        // TODO
        1;
    }
    return nullptr;
}

}